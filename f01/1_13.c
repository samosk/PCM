#include <stdio.h>
int main(void)
{
int var;
scanf("%d", &var);
printf("%d\n", var * 2);
printf("Normal exit.\n");
return 0;
}

// a) Reads a whole number, prints it multiplied by two
// does not change the variable, only prints a doubled value